import {describe, expect, test} from '@jest/globals';
const authRoute = require('../routes/auth.api');

describe("Login Auth Tests", () => {
    test()
})

describe("Delete Auth Tests", () => {
    test()
})